INCLUDE IO1.ASM

S SEGMENT STACK
 DW 128 DUP(?)
S ENDS

C SEGMENT 
 ASSUME DS:NOTHING,CS:C,SS:S 
BEGIN:
 OUTCH '>'
 ININT AX
 MOV BL,10
 DIV BL
 CMP AL,0
 JE PRINT
RPT:
 XOR AH,AH
 DIV BL
 CMP AL,0
JNE RPT
PRINT:
 OUTBUN AH 
 FINISH
C ENDS
  END BEGIN
  END BEGIN